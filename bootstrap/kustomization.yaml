apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - root-application.yaml

patches:
  - target:
      kind: ConfigMap
      name: argocd-cm
      namespace: argocd
    path: argocd-cm-patch.yaml
